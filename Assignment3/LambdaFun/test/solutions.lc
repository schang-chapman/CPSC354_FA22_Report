val empty = \x.
    case x of {
        [] -> 1 ,
        (a:as) -> 0
    } ;;

rec fib = \x.
    case x of {
        0 -> 0 ,
        1 -> 1 ,
        _ -> fib (x-1) + fib (x-2) 
    } ;;

rec length = \x.
    case x of {
        [] -> 0 ,
        (a:as) -> (length as)+1
    } ;;